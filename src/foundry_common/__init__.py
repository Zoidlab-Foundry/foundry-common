"""foundry-common — the shared platform layer for ZoidLab Foundry apps.

One implementation of the five modules every app used to carry as a copy
(auth, entitlements, envelope, llm, pricing) plus the Postgres+RLS core (db).
Fix once, ship everywhere.
"""
__version__ = "0.2.3"
